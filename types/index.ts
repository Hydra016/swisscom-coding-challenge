export type CartItem = {
  quantity: number;
  id: number;
  title: string;
  description: string;
  price: number;
};

export type Product = {
  id: number;
  title: string;
  description: string;
  price: number;
}